# proyecto-copilot
Proyecto de prueba para Estructura de Datos 2

---

Web estática de ejemplo sobre SCRUM creada en `src/`.

Cómo abrirla (Windows PowerShell):

```powershell
# Abrir la página en el navegador predeterminado
ii .\src\index.html
# o
Start-Process .\src\index.html
```

Si quieres que las soluciones sean en un lenguaje concreto (por ejemplo Python), indícalo aquí y añadiré plantillas en `src/` y `tests/`.
```markdown
# proyecto-copilot
Proyecto de prueba para Estructura de Datos 2

```

Web estática de ejemplo sobre SCRUM creada en `src/`.

Cómo abrirla (Windows PowerShell):

```powershell
# Abrir la página en el navegador predeterminado
ii .\src\index.html
# o
Start-Process .\src\index.html
```

Ediciones sugeridas:
- Si quieres que las soluciones sean en un lenguaje concreto (por ejemplo Python), indícalo aquí y añadiré plantillas en `src/` y `tests/`.
# proyecto-copilot
Proyecto de prueba para Estructura de Datos 2
